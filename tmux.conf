# Make it use C-a, similar to screen.
unbind C-b
set -g prefix C-a


# toggle statusbar
bind-key b set-option status

set -g default-terminal "screen-256color"
#set -g history-limit 1000


# enable wm window titles
#set -g set-titles on

# wm window title string (uses statusbar variables)
#set -g set-titles-string "tmux:#I #W"

# session initialization
new-session -s awdesktop -n htop htop
split-window -h -p 75 -t 0 bmon # 'speedometer -rx eth0 -tx eth0'
new-window -n movies 
new-window -n misc 
select-window -t 0


#### statusbar ####
set -g status-interval 1
set -g status-justify centre # center align window list
set -g status-left '#[fg=green] #H #[default]'
set -g status-right '#[fg=blue,bright]Up#(uptime | cut -f 4-5 -d " " | cut -f 1 -d ",") #[default]:: #[fg=cyan]#(date +%Y-%m-%d) #[default]:: #[fg=cyan]#(date +%H:%M) '
#set -g status-right '#[fg=blue,bright]Up#(uptime | cut -f 4-5 -d " " | cut -f 1 -d ",") #[default]:: #[fg=cyan]#(cut -d " " -f 1-4 /proc/loadavg) #[default]#(date +%y-%m-%d) #[default]#(data %H%M) '

# default statusbar colors
set -g status-fg white
set -g status-bg default
set -g status-attr bright

# default window title colors
set-window-option -g window-status-fg white
set-window-option -g window-status-bg default
set-window-option -g window-status-attr dim

# active window title colors
set-window-option -g window-status-current-fg white
set-window-option -g window-status-current-bg default
set-window-option -g window-status-current-attr bright

# command/message line colors
set -g message-fg white
set -g message-bg black
set -g message-attr bright

# THEME
#set -g status-bg black
#set -g status-fg white
#set -g status-interval 60
#set -g status-left-length 30
#set -g status-left '#[fg=green](#S) #(whoami)@#H#[default]'
#set -g status-right '#[fg=yellow]#(cut -d " " -f 1-3 /proc/loadavg)#[default] #[fg=blue]%H:%M#[default]'

source '/home/awidegreen/.local/lib/python3.3/site-packages/powerline/bindings/tmux/powerline.conf'
